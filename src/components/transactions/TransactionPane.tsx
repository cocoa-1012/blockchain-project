import Grid from 'components/base/Grid';

import { styled } from 'stitches.config';

const TransactionPane = styled(Grid, {
  paddingY: '$9',
  boxShadow: '$0',
  borderRadius: 10,
  backgroundColor: '$white100',
});

export default TransactionPane;
