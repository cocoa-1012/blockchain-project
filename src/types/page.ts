export enum PageColorMode {
  dark = 'DARK',
  light = 'LIGHT',
}

export enum PageColorModes {
  dark = 'DARK',
  light = 'LIGHT',
}

// TODO: consolidate these page types where it makes sense
export enum PageTypes {
  auth = 'AUTH',
  minimal = 'MINIMAL',
  maximal = 'MAXIMAL',
}
